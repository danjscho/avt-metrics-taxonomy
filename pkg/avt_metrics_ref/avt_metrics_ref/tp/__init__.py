"""TP cluster — Technical Pipeline (audio capture → downstream write-back).

Pilot library coverage: TP.ASR (transcription) only. Other TP groups
(audio capture, diarisation, summarisation, clinical coding, write-back)
are out of scope for the pilot.
"""
